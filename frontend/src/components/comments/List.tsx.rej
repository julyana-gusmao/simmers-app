diff a/frontend/src/components/comments/List.tsx b/frontend/src/components/comments/List.tsx	(rejected hunks)
@@ -6,6 +6,7 @@ interface User {
   id: number;
   firstName: string;
   lastName: string;
+  profilePicture: string | null;
 }
 
 interface Comment {
@@ -39,7 +40,7 @@ const CommentsList: React.FC<CommentsListProps> = ({ postId, updateComments, pos
   const handleDelete = async (commentId: number) => {
     try {
       await api.delete(`/comments/${commentId}`);
-      fetchComments(); // Atualiza a lista de comentários após a exclusão
+      fetchComments();
     } catch (error) {
       console.error('Erro ao deletar comentário:', error);
     }
@@ -66,8 +67,15 @@ const CommentsList: React.FC<CommentsListProps> = ({ postId, updateComments, pos
   return (
     <div>
       {comments.map((comment) => (
-        <div key={comment.id}>
-          <p>{comment.user.firstName} {comment.user.lastName}</p>
+        <div key={comment.id} className="comment">
+          <div className="comment-header flex items-center">
+            <img
+              src={comment.user.profilePicture ? `http://localhost:3333${comment.user.profilePicture}` : 'default-avatar.png'}
+              alt={`${comment.user.firstName} ${comment.user.lastName}`}
+              className="h-8 w-8 rounded-full object-cover flex-shrink-0"
+            />
+            <p className="ml-2">{comment.user.firstName} {comment.user.lastName}</p>
+          </div>
           <small>{new Date(comment.createdAt).toLocaleString()}</small>
           {editingCommentId === comment.id ? (
             <div>
@@ -96,4 +104,4 @@ const CommentsList: React.FC<CommentsListProps> = ({ postId, updateComments, pos
   );
 };
 
-export default CommentsList;
\ No newline at end of file
+export default CommentsList;
